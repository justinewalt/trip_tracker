<%= form_for @trip do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name, placeholder: 'Trip Name', required: true %>
	<br />
	<%= f.label :start %>
	<%= f.date_field :start %>
	<br />
	<%= f.label :end %>
	<%= f.date_field :end %>
	<br />
	<%= f.submit :submit %>
<% end %>